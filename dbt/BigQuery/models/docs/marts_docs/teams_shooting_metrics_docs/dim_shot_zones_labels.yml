version: 2

models:
  - name: dim_shot_zones_labels
    description: Model creating a numeric label for shot zones lables for reporting
    columns:
      - name: shot_zone_agg
        description: '{{ doc("shot_zone_agg") }}'

      - name: shot_zone_label
        description: '{{ doc("shot_zone_label") }}'

      - name: shot_zone_ordered_num_label
        description: '{{ doc("shot_zone_ordered_num_label") }}'